apiVersion: v1
kind: Secret
metadata:
  name: newborn-nest-secrets
  namespace: newborn-nest
type: Opaque
stringData:
  # Add your secret values here (base64 encoded in production)
  # Example: DATABASE_PASSWORD: "your-password"
  API_KEY: "changeme"
