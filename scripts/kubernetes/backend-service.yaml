apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: newborn-nest
  labels:
    app: newborn-nest
    component: backend
spec:
  type: ClusterIP
  selector:
    app: newborn-nest
    component: backend
  ports:
  - name: http
    port: 5000
    targetPort: 5000
    protocol: TCP
